# Feature: app scaffold

Part of: campaign-v1-schema-canvas.md

Vite + React + TypeScript project in `json-schema-exploded/` with `@xyflow/react`, `elkjs` and `zustand` installed. Full-viewport React Flow canvas; schema files loaded from `schemas/`.

Done when: `npm run dev` shows a pannable, zoomable canvas rendering two hard-coded placeholder nodes and an edge.
